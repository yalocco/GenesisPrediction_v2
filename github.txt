--------git status--------
git status
git status --untracked-files

--------git add--------
git add -u
git add .
git add docker/analyzer/analyze.py
git add docker/analyzer/diff.py
git add docker/analyzer/Dockerfile

--------git commit--------
git commit -m "Fix daily diff generation and stabilize analyzer execution flow"

--------git push--------
git push origin main

--------Author identity unknown
--------fatal: unable to auto-detect email address
git config --global user.name "Masateru Yoshida"
git config --global user.email "your-email@example.com"
git config user.name "Masateru Yoshida"
git config user.email "your-email@example.com"

----git push⑤の確認----
git log -1 --oneline
git status
git push

--------git pull--------
git clone https://github.com/yalocco/GenesisPrediction_v2.git
git pull origin main

--------after git pull--------
docker compose build --no-cache
docker compose run --rm fetcher
docker compose run --rm analyzer

dir .\data\world_politics\analysis
dir .\data\world_politics\analysis\daily_summary_*.json

git status

python -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install matplotlib pandas numpy

visualize_requirements.txtを作成する
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r visualize_requirements.txt

.\.venv\Scripts\python.exe scripts\build_daily_summary_index.py
.\.venv\Scripts\python.exe scripts\plot_regime_timeline.py




